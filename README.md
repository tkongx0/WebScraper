# WebScraper
Custom script written in Python to extract contents from a blog into WordPress blogs.

- Uses selenium to emulate a chrome webdriver (requires downloading the chrome web driver)
- Uses BeautifulSoup to find and scrape specific contents based on html elements
- Pushes data via WP backend API into "block" mode
